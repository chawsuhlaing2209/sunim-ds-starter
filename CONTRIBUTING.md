# Adding a component

1. Read `CLAUDE.md` and `tools.md`.
2. Read `.claude/skills/build/SKILL.md`.
3. Copy the shape of `src/components/Button/`.
4. Run `npm run lint` and `npm run storybook` before handing off.
5. Hand it to QA. Do not verify your own work.
